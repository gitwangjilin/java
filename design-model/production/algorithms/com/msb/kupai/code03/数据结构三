1.归并排序
    第一步将数组递归分割，先吧左边分到底
    然后在右边分割
    在合并
        合并：
            1.先判断两个数据的大小，放进临时数组中
            2.在将第二个数放在临时数组中
                 需要两个循环，一个判断放的第一个数
                            另一个判断放的第二个书
    时间复杂度为n-1常数级别
2.快速排序